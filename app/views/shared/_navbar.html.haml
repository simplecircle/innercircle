.navbar.navbar-fixed-top
  .navbar-inner
    %a.btn.btn-navbar{"data-target" => ".nav-collapse", "data-toggle" => "collapse"}
      %span.icon-bar
      %span.icon-bar
      %span.icon-bar
    .nav-collapse.collapse
      .container-fluid
        .row-fluid
          .span1.hidden-phone
            .brand= link_to(image_tag("logo_small_gray.jpg"), talent_url)
          .span11
            %ul.nav
              %li.divider-vertical
                %h1= @headline
            %ul.nav.pull-right
              %li.divider-vertical
              - if current_user && current_user.role == "admin"
                %li
                  = link_to("Dashboard", talent_url)
              - if current_user
                %li.dropdown
                  %a.display-strong.white.dropdown-toggle{"data-toggle" => "dropdown", :href => "#"}
                    - unless current_user.profile.first_name.blank?
                      = current_user.profile.first_name.capitalize
                    - else
                      - current_user.email
                    %b.caret.caret-black
                  %ul.dropdown-menu
                    %li= link_to "Logout", logout_url
              - else
                %li
                  = link_to "Log In", login_url
