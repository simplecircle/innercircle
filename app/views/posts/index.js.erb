$('#collection').append('<%= j render(@posts) %>');
<% if @posts.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate(@posts) %>');
<% else %>
  $('.pagination').remove();
<% end %>
var handler, options;
handler = null;
options = {
  autoResize: true,
  container: $('.row'),
  offset: 20
};
var firstTen, handler, items;
items = $("#collection li");
firstTen = items.slice(-2);
console.log(firstTen.length);
// $("#collection").append(firstTen.clone());
handler = $("#collection li");
handler.wookmark(options);

